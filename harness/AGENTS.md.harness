# code-compass 路由

本项目的开发流程由 [code-compass](https://github.com/rongmazhong/code-compass) 驱动，
方法论融合 superpowers / gstack / openspec / develop-workflow-rong。

## 命令

| 命令 | 作用 |
|------|------|
| `code-compass use-code-compass` | 注册并启用 skill 库（软链 skills 到 agent 技能目录） |
| `code-compass init` | 初始化 `.harness/` 与 `openspec/`，并注入本段 |
| `code-compass design [name]` | 柏拉图式发问，确定需求范围并生成 spec |
| `code-compass dev [name]` | 基于 spec 进行开发实现 |

## 阶段状态

开发进度记录在 `.harness/workflow-state.json`，阶段链：

```
idea → design → planned → dev → implemented → qa → verified → reviewed → shipped
```

agent 在每一步应优先遵循 code-compass 的 skill 方法论（见 skills/ 下各 SKILL.md）。
